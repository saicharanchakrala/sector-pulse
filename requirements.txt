streamlit>=1.40
feedparser>=6.0.11
vaderSentiment>=3.3.2
pandas>=2.2
numpy>=1.26
plotly>=5.24
websockets>=13.0
requests>=2.32
anthropic>=0.69
pytest>=8.0
pyarrow>=17.0

# Reading the live bars the containerised feed publishes. Only needed when
# SECTOR_PULSE_S3_BUCKET is set; object_store imports it lazily so a purely
# local run never touches it.
boto3>=1.35
ruff==0.16.7
